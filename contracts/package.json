{
  "name": "cipherballot-contracts",
  "version": "1.0.0",
  "description": "CipherBallot – Smart contracts & Hardhat environment for on-chain campus voting",
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.4",
    "dotenv": "^16.4.5"
  }
}
